struct Point {
    double x
    double y
}

fn Point.distance() -> double {
    return sqrt(self.x * self.x + self.y * self.y)
}

fn main() {
    let Point p = Point { 3.0, 4.0 }
    let double d = p.distance()
    io.print("Distance = ", d)  // должно быть 5
}